version: '3'
services:
  image_detection:
    image: ghcr.io/tiltedcube/image_classification:latest
    ports:
      - "8000:8000"
    volumes:
        - ./models:/app/models:rw
    environment:
      - MODEL_NAME
      - ACCESS_TOKEN